import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Expenses — SchoolPurse" };

export default function ExpensesPage() {
  return (
    <PagePlaceholder
      title="Expenses"
      description="Log and categorise school expenditure — payroll, utilities, maintenance, supplies, operations."
      phase="Lands in Phase 3"
    />
  );
}
