import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Payments — SchoolPurse" };

export default function PaymentsPage() {
  return (
    <PagePlaceholder
      title="Payments"
      description="Record cash payments at the office, issue sequential receipts, and search through the ledger."
      phase="Lands in Phase 2"
    />
  );
}
