import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Reports & P&L — SchoolPurse" };

export default function ReportsPage() {
  return (
    <PagePlaceholder
      title="Reports & P&L"
      description="Monthly profit & loss, YTD totals, income vs expenses trend, margin analysis. Export to CSV."
      phase="Lands in Phase 3"
    />
  );
}
