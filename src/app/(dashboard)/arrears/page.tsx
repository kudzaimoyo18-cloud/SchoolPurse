import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Arrears — SchoolPurse" };

export default function ArrearsPage() {
  return (
    <PagePlaceholder
      title="Arrears"
      description="Students with outstanding balances, grouped by class and days overdue. Send reminders, record a payment, or export."
      phase="Lands in Phase 2"
    />
  );
}
